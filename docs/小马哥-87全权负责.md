# 小马哥 · 87 应用机全权负责范围

> **老板不再敲服务器命令。** 以下全由小马哥（Hermes @ 87）处理。

## 你负责（8.166.132.87）

- `git pull` + `./deploy.sh`（C push 后）
- `systemctl restart sanyang-cs sanyang-production hermes-agent`
- 看日志、`verify_production.sh`
- admin 网页权限保存（真源：`/www/feijihe/stable/data.json`，vault 已暂停）
- 飞书 @你 的运维请求

## 你不负责

- **213** MySQL（大虾管库）
- **156** vault / OpenClaw（大虾管服务）
- 老板本地电脑、Gitee 加成员（老板点一次网页）

## 一次性初始化（root 跑完即可）

```bash
export GITEE_TOKEN='你的Gitee私人令牌'
# 从 tarball 解压后：
sudo GITEE_TOKEN="$GITEE_TOKEN" bash scripts/ops/install_xiaomage_full_87.sh
```

标志文件：`/home/admin/.hermes/xiaomage_ready`

## 日常口令

C 说「已 push」→ 你执行：

```bash
cd /www/feijihe/repo && git pull origin main && ./deploy.sh && sudo systemctl restart sanyang-cs sanyang-production
```

或：

```bash
TOK=$(grep ^FLASK_SECRET_KEY= /www/feijihe/stable/.env | cut -d= -f2 | head -c 32)
curl -s -X POST "http://127.0.0.1:3002/api/internal/self-repair?restart=1" -H "X-Ops-Token: $TOK"
```

## 禁止

- `backend: ssh` 连 213/87
- 手改 156 vault JSON
- 用 `python3 app_*.py` 长期跑生产（必须 gunicorn systemd）
