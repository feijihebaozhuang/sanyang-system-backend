---
description: 改完代码后必须提交并推送到远程
alwaysApply: true
---

# 改完即推送

在本仓库完成任何有意义的修改后（代码、配置、文档、部署脚本等）：

1. 使用 `git status` / `git diff` 确认变更范围
2. 将相关文件 `git add` 并 `git commit`（信息简洁，说明「为什么」）
3. **必须** `git push` 到当前跟踪的远程分支（通常为 `origin/main`）

除非用户在本轮对话中明确说「不要提交」「不要推送」「只改本地」，否则不要只改文件不推送。

推送成功后简要告知提交哈希与分支；若推送失败，继续排查并重试，不要留给用户手动推送。
